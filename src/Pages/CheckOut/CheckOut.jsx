import React from 'react';
import PropTypes from 'prop-types';

const CheckOut = props => {
    return (
        <div>
            <h2>
                Check Out
            </h2>
        </div>
    );
};

CheckOut.propTypes = {
    
};

export default CheckOut;